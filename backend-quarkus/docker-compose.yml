services:
  oracle-db:
    image: gvenzl/oracle-free:slim
    container_name: oracle-inventory
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=123
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data: